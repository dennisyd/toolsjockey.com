import React from 'react';
import FileHashVerifier from '../../components/tools/FileHashVerifier';

const FileHashVerifierPage: React.FC = () => {
  return <FileHashVerifier />;
};

export default FileHashVerifierPage; 